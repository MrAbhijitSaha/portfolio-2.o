import ContactForm from "@/components/ContactPage/ContactForm";
import { ToastContainer } from "react-toastify";

const page = () => {
	return (
		<>
			<ContactForm />
			<ToastContainer />
		</>
	);
};

export default page;
